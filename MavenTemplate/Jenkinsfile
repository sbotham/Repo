pipeline {
    agent any
  tools {
        maven 'Maven 3.5.2'
        jdk 'Java 8u152'
    }

    stages {
        stage('Build') {
            steps {
                echo 'Building..I updated this...and again...1'
                sh 'mvn -f MavenTemplate/ -Dmaven.test.failure.ignore=true install'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
